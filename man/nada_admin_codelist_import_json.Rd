% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codelists.R
\name{nada_admin_codelist_import_json}
\alias{nada_admin_codelist_import_json}
\title{Import one codelist and its items from JSON}
\usage{
nada_admin_codelist_import_json(
  payload = NULL,
  file = NULL,
  dry_run = NULL,
  overwrite = NULL,
  api_key = NULL,
  api_base_url = NULL
)
}
\arguments{
\item{payload}{Named list with `codelist` (nested object with idno/name/items, etc.),
optional `overwrite` and `dry_run` booleans, OR a legacy flat list accepted by the API
when it is unambiguous.}

\item{file}{Path to a JSON file (mutually exclusive with `payload`).}

\item{dry_run}{If not `NULL`, sets body field `dry_run`.}

\item{overwrite}{If not `NULL`, sets body field `overwrite` (replace items on matched list).}

\item{api_key, api_base_url}{See `nada_admin_dsd_list`.}
}
\value{
list with status_code and response
}
\description{
Wraps POST /api/admin/codelists/import_json. Body fields `overwrite` and `dry_run`
are sent in the JSON body only (no query parameters).
}
