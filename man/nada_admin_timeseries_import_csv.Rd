% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timeseries_observations.R
\name{nada_admin_timeseries_import_csv}
\alias{nada_admin_timeseries_import_csv}
\title{Import observations from a CSV file into MongoDB}
\usage{
nada_admin_timeseries_import_csv(
  idno,
  file,
  delimiter = ",",
  mapping = NULL,
  ensure_unique_index = TRUE,
  api_key = NULL,
  api_base_url = NULL
)
}
\arguments{
\item{idno}{Study idno (`surveys.idno`).}

\item{file}{Path to the CSV file to upload.}

\item{delimiter}{CSV field delimiter. Defaults to ",". Use "\\t" for tab.}

\item{mapping}{Named list mapping CSV header -> DSD component name, e.g.
`list(country = "REF_AREA", year = "TIME_PERIOD", value = "OBS_VALUE")`.
Headers absent from the mapping are used as-is.}

\item{ensure_unique_index}{When TRUE (default), the unique `key_hash` index
is created before insert. Set FALSE to allow duplicates through.}

\item{api_key}{Optional API key; defaults to `nada_get_api_key()`.}

\item{api_base_url}{Optional API base URL; defaults to `nada_get_api_url()`.}
}
\value{
list with `status_code` and `response`.
}
\description{
Wraps POST /api/admin/timeseries/data/{idno}/import.
}
\details{
The study must already be linked to a global DSD (see
`nada_admin_timeseries_set_dsd`). The server:
  - Resolves the DSD from `surveys.data_structure_id`.
  - Ensures Mongo indexes (including unique `key_hash` by default).
  - Reads the CSV, applies the optional header `mapping`, and rejects any
    post-mapping header that is not a DSD component.
  - Builds observation documents and bulk-inserts; empty cells become NULL.

HTTP responses:
  - 201 `{ status, result: { inserted, lines_read } }`.
  - 409 on duplicate `key_hash` for any row.
  - 400 on validation / unknown mapped field / missing DSD link.
}
\examples{
\dontrun{
  nada_admin_timeseries_import_csv(
    idno    = "prices-study",
    file    = "observations.csv",
    mapping = list(country = "REF_AREA",
                   year    = "TIME_PERIOD",
                   value   = "OBS_VALUE")
  )
}

}
